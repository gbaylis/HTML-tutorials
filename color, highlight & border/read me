HTML / CSS page
- how to link a HTML to a CSS page
- how to create a tag, so modify a specific line of text
- add a border to text
- change the font family (as in, what type of font the text will be in)
- highlight text
